import { SessionController } from '@/presentation/controllers/session/SessionController'

interface SutTypes {
  sut: SessionController,
  authent
}


describe('Session Controller', () => {
  test ('Should call Authentication with correct values', async () => {

  })
});
